spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    driverClassName: org.h2.Driver
    password:
  h2:
    console:
      enabled: true
  shell:
    interactive:
      enabled: false

logging:
  level:
    root: INFO
